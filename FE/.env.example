# Where the API lives. Vite inlines VITE_ variables into the bundle at build
# time, so this must be set before `npm run build`, not at runtime.
#
# Leave it unset for local development - the code falls back to
# http://localhost:3000/api on its own.
VITE_API_ROOT=https://your-api.onrender.com/api
